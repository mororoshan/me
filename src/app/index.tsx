import App from './App'

/**
 * App entry component – root UI rendered by the app shell.
 * Routing and global providers will be added in later phases.
 */
export default function AppEntry() {
  return <App />
}
