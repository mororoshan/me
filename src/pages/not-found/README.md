# Not found page

404 page for unknown routes (`*`). Shown when no route matches.

## Translation keys

- `notFound.title` — heading (e.g. "Not found")
- `notFound.message` — short message

## Dependencies

- `react-i18next` (`useTranslation`)
