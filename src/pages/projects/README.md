# Projects page

Projects list at `/projects`. Shows page title and empty state (or project grid widget when Phase 8 is applied).

## Translation keys

- `projects.title` — page heading
- `projects.empty` — empty state message

## Dependencies

- `react-i18next` (`useTranslation`)
- `antd` (`Typography`)
