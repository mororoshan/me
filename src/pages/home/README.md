# Home page

Landing page at `/`. Shows a welcome title and short intro.

## Translation keys

- `home.title` — page heading (e.g. "Welcome")
- `home.intro` — intro paragraph

## Dependencies

- `react-i18next` (`useTranslation`)
- `antd` (`Typography`)
