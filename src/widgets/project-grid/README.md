# Project grid widget

Responsive grid of project cards. Imports the project list from `entities/project` and renders a `ProjectCard` for each. Used on the Projects page.

## Props

None.

## Dependencies

- `entities/project` (`projects` list)
- `widgets/project-card` (`ProjectCard`)
